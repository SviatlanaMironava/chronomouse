{
  "name": "@er1c/chronomouse",
  "version": "1.0.2",
  "description": "These package was forked for fix on not working Jordan and Greenland country",
  "scripts": {
    "minifyJS": "uglifyjs chronomouse.js --compress --mangle --warn --output chronomouse.$(npm run version:extract --silent).min.js",
    "production": "rm -f chronomouse.$(npm run version:extract --silent).min.js && npm run version:bump --silent && npm run minifyJS --silent",
    "version:bump": "npm version patch --no-git-tag-version --silent",
    "version:extract": "cat package.json | grep version | head -1 | awk -F: '{ print $2 }' | sed 's/[,]//g' | tr -d '[[:space:]]' | tr -d \"\\\"\" "
  },
  "author": {
    "name": "er1c"
  },
  "main": "chronomouse.1.0.2.min.js",
  "repository": {
    "url": "https://github.com/er1cAk/chronomouse.git"
  },
  "devDependencies": {},
  "dependencies": {
    "uglify-js": "^3.10.2"
  },
  "license": "MIT"
}
